vagrant -v
vagrant box add ubuntu/trusty64
vagrant init ubuntu/trusty64
vagrant up
vagrant ssh
vagrant halt
vagrant destroy

vi Vagrantfile

vagrant reload
vagrant ssh


sudo apt-get update
sudo apt-get install nginx
sudi service nginx start

<Edit your local hosts and assign  a domain to Vagrant's IP>
